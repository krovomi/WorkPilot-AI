# Test dependencies for the WorkPilot AI Framework
# Install with: pip install -r tests/requirements-test.txt

# Testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-timeout>=2.0.0

# Mocking
pytest-mock>=3.0.0

# For testing async code
anyio>=4.0.0

# HTTP client used by the Copilot/OpenAI/Windsurf agent clients. Tests exercise
# these code paths directly (mocked sessions) and construct aiohttp exceptions
# (e.g. ClientConnectionError) to verify the request-retry budgets, so aiohttp
# must be importable in the test environment even though it is an optional
# runtime dependency loaded lazily in production.
aiohttp>=3.9.0

# docintel paints secrets out of screenshots with Pillow when it is installed,
# and withholds the image when it is not. Both paths are tested; this makes
# the first one run in CI rather than skip.
Pillow>=10.0.0

# docintel reads a PDF's text layer and renders a scanned one's pages with
# pypdfium2 when it is installed (a wheel on every platform, no system
# dependency), and says `no-pdf-backend` when it is not. Installed here so the
# real text layer and the real rendering are what the tests exercise.
pypdfium2>=4.30.0

# For testing validation models (required by backend code)
pydantic>=2.0.0

# Code coverage
coverage>=7.0.0

# For snapshot/approval testing (optional)
# pytest-snapshot>=0.9.0

# Type checking (for running mypy on tests)
mypy>=1.0.0
types-toml>=0.10.0
